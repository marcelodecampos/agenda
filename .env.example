# Copie para .env e ajuste os valores antes de subir o ambiente local (docker compose up -d)

POSTGRES_DB=agenda
POSTGRES_USER=agenda
POSTGRES_PASSWORD=changeme
POSTGRES_PORT=5432

KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=changeme
KEYCLOAK_PORT=8080
